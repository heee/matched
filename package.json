{
  "name": "matched",
  "private": true,
  "type": "module",
  "description": "No build step — this file exists so Node treats the app's .js files as ES modules for local scripts (static server, icon generation, tests).",
  "scripts": {
    "serve": "node scripts/static-server.cjs",
    "test": "node --test tests/*.test.mjs",
    "icons": "node scripts/generate-icons.cjs",
    "bundle-worker": "node scripts/bundle-worker.cjs",
    "validate-worker": "node scripts/validate-worker.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
